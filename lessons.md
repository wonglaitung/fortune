# 经验教训

> **版本**：v5.5 (2026-05-05) | **状态**：当前有效

---

## 一、核心警告

### 1. IC 计算必须与训练一致 ⭐（新增 2026-05-05）

**问题**：IC 计算使用二元标签而非实际收益率，且收益率计算方式与训练不一致

**原因**：
- 原代码：`ic = probability.corr(Label)`（Label 是 0/1 二元标签）
- 业界标准：`ic = probability.corr(actual_return)`（实际收益率）
- 训练时：`Future_Return = Close[t+horizon] / Close[t] - 1`（累积收益）
- 验证时原代码：`actual_return = Close.pct_change().shift(-horizon)`（单日收益）

**解决**：
```python
# 正确：与训练一致的累积收益率
df['actual_return'] = df['Close'].shift(-horizon) / df['Close'] - 1
ic = df['probability'].corr(df['actual_return'])
```

**教训**：
- IC 应计算预测概率与实际收益率的相关性，而非二元标签
- 验证时的收益率计算必须与训练时一致
- 二元标签丢失了收益幅度信息，导致 IC 计算不准确

**代码**：`ml_services/walk_forward_validation.py:418-445`

### 2. 市场级特征需与股票特征交叉 ⭐（更新 2026-05-07）

**问题**：市场级特征（如 HSI_Return、VIX）对所有股票同值，无法区分个股

**原因**：
- 市场级特征在同一天对所有股票具有完全相同的值
- 直接使用会导致模型无法学习个股差异
- `MARKET_LEVEL_FEATURES` 列表与特征模块不同步，导致遗漏

**历史问题**（2026-05-07 发现）：
- `RegimeDetector` 新增 Tier 1 特征（5个）未加入排除列表
- `GARCH_Persistence` 未加入排除列表
- 日历特征（22个）完全遗漏
- 导致 `HSI_Regime_Momentum` 等特征直接进入模型，降低区分能力

**解决**：
- 使用 `_build_market_level_features()` 动态构建，自动同步特征模块
- 与网络社区特征交叉：`HSI_Return_1d * net_community_id`
- 不同社区的股票对同一市场信号有不同权重

```python
def _build_market_level_features():
    """动态构建，确保与特征模块自动同步"""
    features = []
    # 从特征模块动态获取
    for feat in RegimeDetector.get_feature_names():
        features.append(f'HSI_{feat}')
    features.extend(GARCHVolatilityModel.get_feature_names())
    features.extend(CalendarFeatureCalculator.get_feature_names())
    return features
```

**教训**：
- 市场级特征不能直接用于个股预测
- 必须通过与股票特定特征交叉来引入差异化
- **新增特征模块时，必须在 `get_feature_names()` 中定义，确保自动同步**
- 避免手动维护列表，使用动态构建

**当前状态**：60 个市场级特征，动态构建，自动同步

**代码**：`ml_services/ml_trading_model.py:68-115`（动态构建）、`ml_services/ml_trading_model.py:2696-2785`（交叉方法）

### 3. 股票代码格式必须统一 ⭐（新增 2026-05-05）

**问题**：股票代码格式不一致（如 `02382.HK` vs `2382.HK`）导致特征匹配失败

**解决**：
- 统一移除前导零：`02382.HK` → `2382.HK`
- 检查 `STOCK_SECTOR_MAPPING` 和 `TRAINING_STOCKS` 中的一致性

**教训**：
- 股票代码格式必须在所有配置中保持一致
- 前导零可能导致网络特征、基本面数据匹配失败

**代码**：`config.py`

### 4. 数据泄漏阈值因模型而异 ⭐（更新 2026-05-05）

**问题**：恒指和个股的数据泄漏阈值不同

**原因**：
- 恒指是"均值"，噪声被对冲，预测更容易
- 个股受公司特定事件影响，预测难度更高
- 因此恒指的"正常准确率上限"高于个股

**阈值标准**：

| 模型类型 | Walk-forward 准确率正常范围 | 数据泄漏信号 |
|---------|---------------------------|-------------|
| 个股 | 50-60% | >65% |
| 恒指 | 60-82% | >80%（增强模型） |

**教训**：
- 不要用同一标准衡量不同类型的模型
- 恒指 80% 准确率是正常的，个股 65% 就要警惕

### 2. 特征冗余清理需谨慎 ⭐（新增 2026-04-28）

**问题**：清理冗余特征后夏普比率下降 15-20%

**验证数据**：

| 指标 | 清理前 (1045) | 清理后 (730) | 变化 |
|------|--------------|--------------|------|
| 准确率 | 54.82% | 57.15% | +2.33% ✅ |
| 夏普比率 | 1.0221 | 0.8122 | -20.5% ⚠️ |
| 稳定性（夏普标准差） | 0.7597 | 0.5844 | +23% ✅ |

**教训**：
- **r=1.0 的"冗余"特征可能有预测价值**：在不同市场环境下可能有差异化信号
- CatBoost 对特征冗余有天然容错机制，正则化可自动处理
- 交叉特征减少会丢失非线性交互信息
- 清理特征后需重新调参，不能直接使用旧参数

**建议**：
- 清理前必须进行对比验证
- 保留性能最好的版本
- 特征工程变更后应调整模型参数

### 3. 恒指与个股的预测准确率差距大 ⭐（更新 2026-04-28）

**问题**：恒指预测准确率显著高于个股，个股预测接近随机

**验证数据**：

| 指标 | 恒指 | 个股平均 | 差异 |
|------|------|---------|------|
| 1天预测正确时 → 20天也正确 | **83.56%** | **58.20%** | +25.36% |
| 5天预测正确时 → 20天也正确 | **81.95%** | **61.86%** | +20.09% |
| 1天+5天都正确时 → 20天也正确 | **81.49%** | **61.74%** | +19.75% |
| 假突破(101)准确率 | **95.00%** | **54.58%** | +40.42% |
| 5d预测概率与20d实际方向相关性 | +0.35 | +0.03 | 个股相关性弱 |

**教训**：
- 恒指是"均值"，个股噪声被对冲，预测更容易
- 个股所有模式准确率接近随机水平（50%~62%）
- **不能将恒指策略直接套用于个股**
- 个股预测信号不可靠，需结合恒指确认趋势

**代码**：`ml_services/analyze_three_horizon_relationships.py`、`ml_services/analyze_stock_causal_chain.py`

### 4. 训练窗口不是越大越好 ⭐

**问题**：样本/特征比 7.6x 低于推荐 10x，但增加窗口反而降低准确率

**验证**：12个月 60.11% > 18个月 56.57% > 24个月 54.85%

**教训**：
- 港股近年结构性变化，近期数据比更多历史数据更有预测价值
- Walk-forward 机制保证时效性，弥补样本量不足
- CatBoost 正则化能有效处理高维问题

### 5. 1d 预测有"反向指标"效应 ⭐

**问题**：5个1d预测全错时，5d准确率高达 87.5%（高于全对的 67.4%）

**教训**：
- 1d预测噪声大，全错可能是"反向信号"
- 短期预测的大幅偏离可作为中长期趋势的辅助判断
- 但样本量较小（16次），需谨慎参考

### 6. 性能监控提前评估 Bug ⭐

**问题**：`fetch_price` 容错机制导致提前评估未来到期的预测

**教训**：
- "容错"机制可能引入隐蔽 Bug
- 时间相关逻辑必须有边界检查

**代码**：`ml_services/performance_monitor.py:179-182`

### 7. 特征缓存版本控制 ⭐

**问题**：新增特征后，旧缓存缺少新特征列，导致预测失败或使用错误特征

**解决**：
- 缓存验证时检查必需特征列是否存在
- 缺少新特征时标记缓存无效，重新计算

**教训**：
- 特征工程迭代时，缓存验证逻辑必须同步更新
- 检测特征列而非仅检测缓存文件存在性

**代码**：`ml_services/ml_trading_model.py`（CatBoost 缓存验证逻辑）

### 8. CatBoost 分类特征 NaN 处理 ⭐（新增 2026-04-27）

**问题**：`predict_proba` 函数缺少分类特征 NaN 处理，导致测试数据预测失败

**现象**：所有 Walk-forward Fold 验证失败，报错 `cat_features must be integer or string, real number values and NaN values should be converted to string`

**原因**：
- 训练时对分类特征进行了 `fillna('unknown')` 和 `LabelEncoder` 转换
- 但 `predict_proba` 函数缺少相同处理

**解决**：
```python
# 在 predict_proba 中添加分类特征处理
for col in self.categorical_encoders.keys():
    if col in test_df.columns:
        test_df[col] = test_df[col].fillna('unknown').astype(str)
        encoder = self.categorical_encoders[col]
        test_df[col] = test_df[col].apply(
            lambda x: encoder.transform([x])[0] if x in encoder.classes_ else -1
        )
```

**教训**：
- 训练和预测的预处理逻辑必须一致
- 分类特征的 NaN 值必须在进入 CatBoost Pool 之前处理
- 建议使用统一的预处理管道

**代码**：`ml_services/ml_trading_model.py:4937-4948`

### 9. 大模型 Prompt 数据格式 ⭐

**问题**：大模型混淆不同周期的概率（1天 vs 20天）

**解决**：分离数据用途，大模型只看必要数据

**教训**：Prompt 中的数据格式应尽可能简单、唯一

**代码**：`comprehensive_analysis.py:695-930`

### 10. 数据源一致性问题 ⭐

**问题**：实时价格（分钟数据）和收益率（日线数据）来自不同数据源，导致不一致

**解决**：统一使用腾讯财经 API，失败回退 yfinance

**教训**：价格和收益率必须基于同一数据源

**代码**：`comprehensive_analysis.py:1488-1530`

### 11. 重构代码遗漏功能 ⭐

**问题**：重构时删除了传导模式列

**教训**：重构时必须逐一核对原有功能

---

## 二、快速参考

### 数据泄漏检测

**个股模型**：

| Walk-forward 准确率 | 判断 | 行动 |
|---------------------|------|------|
| 50-55% | 正常 | 业界水平 |
| 55-60% | 优秀 | 接近天花板 |
| **>65%** | **可疑** | 检查数据泄漏 |

**恒指增强模型**：

| Walk-forward 准确率 | 判断 | 行动 |
|---------------------|------|------|
| 60-75% | 正常 | 恒指噪声低 |
| 75-80% | 优秀 | 接近上限 |
| **>80%** | **可疑** | 检查数据泄漏 |

> **注意**：训练时CV准确率（62-71%）通常高于Walk-forward准确率，这是正常现象。数据泄漏的信号是 **Walk-forward准确率异常高**（个股 >65%，恒指 >80%），而非训练CV准确率高。

### 模型可信度

| 模型 | 可信度 | 推荐 |
|------|--------|------|
| CatBoost 20天 | ⭐⭐⭐⭐⭐ | **推荐** |
| CatBoost 5天 | ⭐⭐⭐ | 谨慎使用 |
| CatBoost 1天 | ⭐ | 不推荐（过拟合） |
| LSTM/Transformer | ⭐ | 不推荐（F1≈0） |

### 验证方法

| 方法 | 可信度 | 说明 |
|------|--------|------|
| **Walk-forward** | ✅ 唯一可信 | 每个 fold 重新训练 |
| 简单评估 | ❌ 不可信 | 结果虚高 |

### 样本/特征比例

| 比例 | 状态 | 建议 |
|------|------|------|
| < 10:1 | 过拟合风险 | 增加样本或减少特征 |
| 15:1 | 可接受 | 当前状态 |
| 50:1+ | 理想 | 推荐 |

---

## 三、交易策略

### 异常检测策略

| 策略 | 收益 | 胜率 | 操作 |
|------|------|------|------|
| IF high | -3.04% | 43% | 🔴 减仓 |
| 价格异常 + 当日下跌 | **+4.12%** | **72%** | 🟢 抄底 |

**注意**：股票策略不适用于加密货币

### 因果链传导（2026-04-29 验证）

| 链 | 恒指 | 个股 | 差异原因 |
|----|------|------|---------|
| 5d预测概率与20d实际方向相关性 r | **+0.35** | **+0.03** | 个股相关性弱 |
| 模型 vs 投票差距 | +16% | +13% | 一致：模型更优 |
| 1天预测正确时→20天也正确 | **83.56%** | **58.20%** | 个股传导弱 |
| 5天预测正确时→20天也正确 | **81.95%** | **61.86%** | 个股传导弱 |

**核心教训**：个股预测准确率接近随机（50%左右），不可单独依赖个股预测信号

### 三周期策略（恒指，2026-04-29 验证）

| 模式 | 名称 | 准确率 | 建议 |
|------|------|--------|------|
| 101 | 假突破 | **95.00%** | ⭐⭐⭐⭐⭐ 最优 |
| 010 | 反弹失败 | **85.98%** | ⭐⭐⭐⭐⭐ 次优 |
| 001 | 下跌中继 | **84.00%** | ⭐⭐⭐⭐ 做多 |
| 111 | 一致看涨 | **80.62%** | ⭐⭐⭐⭐ 买入 |
| 000 | 一致看跌 | **79.57%** | ⭐⭐⭐⭐ 做空 |

### 三周期策略（个股，2026-04-28 验证）

| 模式 | 名称 | 准确率 | 说明 |
|------|------|--------|------|
| 111 | 一致看涨 | **55.77%** | 接近随机 |
| 110 | 震荡回调 | **60.68%** | 样本少 |
| 101 | 假突破 | **54.58%** | 与恒指差距大 |
| 其他模式 | - | ~55% | 接近随机 |

**关键发现**：个股所有模式准确率接近随机水平，恒指策略不可直接套用于个股

### 板块轮动

| 效应 | 有效性 |
|------|--------|
| 周期/防御轮动 | ⭐⭐⭐⭐ 有效 |
| 动量/反转策略 | ⭐⭐ 不推荐 |

---

## 四、开发规范

### CatBoost 参数调优（特征数量变化时）

**场景**：特征数量从 1045 减少到 730，需要调整参数

**调整原则**：

| 参数 | 特征减少时调整方向 | 原因 |
|------|-------------------|------|
| n_estimators | 增加 ↑ | 特征减少需要更多树来捕捉信息 |
| depth | 增加 ↑ | 允许更复杂的决策边界 |
| l2_leaf_reg | 降低 ↓ | 特征减少后过拟合风险降低 |
| subsample | 增加 ↑ | 特征减少可以增加行采样 |
| colsample_bylevel | 增加 ↑ | 特征减少可以增加列采样 |

**当前 20天模型参数（730 特征，2026-04-29 手动调优最优版）**：

| 参数 | 值 | 说明 |
|------|-----|------|
| n_estimators | 600 | 树数量（增加） |
| depth | 7 | 树深度（增加） |
| learning_rate | 0.03 | 学习率（降低） |
| l2_leaf_reg | 2 | L2 正则化（降低） |
| subsample | 0.75 | 行采样（增加） |
| colsample_bylevel | 0.75 | 列采样（增加） |

**调优效果对比（2026-04-29）**：

| 指标 | 旧参数 | 手动调优 | 系统调优 | 目标 | 最佳 |
|------|--------|---------|---------|------|------|
| 准确率 | 57.15% | 54.93% | 55.78% | >58% | 旧参数 |
| 夏普比率 | 0.8122 | **0.9059** | 0.8802 | >0.9 | 手动调优 ✅ |
| 最大回撤 | -0.53% | **-0.20%** | -0.22% | - | 手动调优 ✅ |
| 稳定性 | 0.5844 | 0.4770 | **0.3962** | - | 系统调优 |

**最终选择**：手动调优参数（夏普比率达标，最大回撤最优）

**教训**：
- 特征减少后应降低正则化强度
- 特征数量变化后必须重新验证性能
- 使用 Walk-forward 验证而非训练 CV 评估真实性能
- **降低学习率 + 增加树数量** 可提升夏普比率和稳定性
- **增加树深度** 可捕捉更复杂模式，但可能略微降低准确率
- **系统调优需谨慎**：快速验证结果可能过于乐观，需完整验证确认

### 调优脚本改进经验（2026-04-29）

**问题**：快速验证结果过于乐观，完整验证后泛化不足

**改进措施**：

| 改进项 | 旧版 | 新版 | 原因 |
|--------|------|------|------|
| 股票数量 | 10只 | 20只（快速）/59只（完整） | 更多样本减少过拟合 |
| 验证fold数 | 3个 | 4个（快速）/8个（完整） | 更全面评估 |
| 验证方法 | TimeSeriesSplit | Walk-forward风格 | 更接近真实预测 |
| 稳定性指标 | 仅准确率std | 准确率std + 夏普std | 全面评估稳定性 |
| 早停机制 | 无 | 连续5次无改进 | 避免无效搜索 |

**使用建议**：
```bash
# 标准调优（推荐）
python3 ml_services/hyperparameter_tuner.py --horizon 20 --n-iter 30

# 快速测试
python3 ml_services/hyperparameter_tuner.py --horizon 20 --n-iter 15 --quick

# 完整调优（耗时较长但最可靠）
python3 ml_services/hyperparameter_tuner.py --horizon 20 --n-iter 50 --full
```

**教训**：
- 调优脚本评估结果需用完整 Walk-forward 验证确认
- 快速模式适合初步探索，完整模式用于最终确认
- 早停机制可节省时间，但需设置合理阈值

### 阈值配置

| 用途 | 阈值 | 说明 |
|------|------|------|
| **方向预测** | `0.5` | 概率 > 0.5 预测上涨 |
| 置信度分级 | `0.65/0.55` | 信号强弱，不影响方向 |

### 数据泄漏防护

```python
# ❌ 错误：使用当日数据
df['Feature'] = df['Close'].pct_change()

# ✅ 正确：使用昨日数据
df['Feature'] = df['Close'].pct_change().shift(1)
```

**高风险特征**：所有 `.rolling()` 计算的特征必须 `.shift(1)`

### 特征缓存

| 项目 | 说明 |
|------|------|
| 位置 | `data/feature_cache/` |
| 有效期 | 7天 |
| 加速 | **170x** |

### 交易日计算

```python
# ❌ 错误：自然日
target_date = date + timedelta(days=horizon)

# ✅ 正确：交易日
df = ak.tool_trade_date_hist_sina()
```

---

## 五、常见错误

| 错误 | 后果 | 修复 |
|------|------|------|
| 动量分析数据泄漏 | 虚高收益 | `future_return.shift(-5)` |
| 回撤计算错误 | -84% | 使用批次回撤 |
| 夏普比率年化错误 | 0.29 | 月度收益年化 |
| Walk-forward 样本不足 | 38% | 样本 ≥ 200 |
| 配置与模型不一致 | 误导决策 | 统一数据来源 |
| 流程重复计算 | 性能浪费 | 使用缓存 |
| 交易日 vs 自然日 | 评估偏差 | 使用交易日历 |

---

## 六、更新日志

| 日期 | 版本 | 变更 |
|------|------|------|
| 2026-05-05 | v5.5 | 添加：IC计算一致性、市场级特征交叉、股票代码格式统一、百分位预测效果评估、详细分析结果保存 |
| 2026-04-28 | v5.3 | 添加：数据泄漏阈值因模型而异（个股>65%，恒指>80%）；修复编号 |
| 2026-04-28 | v5.2 | 添加：特征冗余清理经验、CatBoost参数调优经验 |
| 2026-04-27 | v5.1 | 添加：特征缓存版本控制经验 |
| 2026-04-27 | v5.0 | 添加：恒指vs个股因果链差异、训练窗口验证、1d反向指标效应 |
| 2026-04-23 | v4.1 | 添加：近期指标权重配置经验 |
| 2026-04-23 | v4.0 | 重构：精简至核心内容 |
| 2026-04-23 | v3.16 | 添加：性能监控提前评估 Bug、时间窗口设计 |
| 2026-04-22 | v3.15 | 添加：重构代码时遗漏重要列问题 |
| 2026-04-22 | v3.14 | 添加：大模型 Prompt 数据格式问题、数据源一致性问题 |
| 2026-04-20 | v3.13 | 更新：个股三周期完整模型验证 |

### 百分位预测效果评估 ⭐（新增 2026-05-05）

**场景**：评估模型头部预测的选股能力

**方法**：按预测概率截面排名，计算 Top 1%/5%/10%/20% 股票的实际收益

**解读标准**：

| 分位 | 超额收益 > 0 | 超额收益 < 0 | 说明 |
|------|-------------|-------------|------|
| Top 1% | ✅ 有效 | ❌ 无效 | 最高置信预测 |
| Top 5% | ✅ 有效 | ❌ 无效 | 头部预测 |
| Top 10% | ✅ 有效 | ❌ 无效 | 中等置信 |
| Top 20% | ✅ 有效 | ❌ 无效 | 较宽范围 |

**当前模型表现**（CatBoost Ranker 20d）：

| 分位 | 平均收益 | 超额收益 | 评估 |
|------|---------|---------|------|
| Top 1% | 10.72% | -9.40% | ⚠️ 胜率过半但超额收益负 |
| Top 5% | 31.81% | **+11.69%** | ✅ 有效 |
| Top 10% | 19.77% | -0.35% | ❌ 无效 |
| Top 20% | 15.18% | -4.94% | ❌ 无效 |

**教训**：
- Top 5% 是当前模型的有效预测范围
- Top 1% 样本量小（140），统计不稳定
- 超额收益递减说明模型有一定选股能力，但范围有限

**代码**：`ml_services/walk_forward_validation.py:560-695`

### Walk-forward 详细分析结果保存 ⭐（新增 2026-05-05）

**新增保存文件**：

| 文件 | 内容 | 用途 |
|------|------|------|
| `validation_summary.json` | 汇总信息（评分、IC、Rank IC） | 快速查看 |
| `fold_metrics_detail.json` | 各 fold 详细指标 | 稳定性分析 |
| `prediction_distribution.json` | 预测分布统计 | 避免"全涨全跌" |
| `error_analysis.csv` | False Positive/False Negative | 错误模式分析 |
| `confidence_return_breakdown.json` | 置信度与收益关系 | 置信度有效性 |
| `percentile_performance.json` | 百分位预测效果 | 选股能力评估 |

**教训**：
- 详细结果保存便于后续分析和对比
- IC/Rank IC 是选股能力的核心指标
- 错误分析可发现模型弱点

---

## 七、分析器设计经验

### 近期指标权重配置

**场景**：风险回报率分析需更关注近期表现

**方案**：添加 `recent_bias` 参数动态调整权重

```bash
# 参数说明
--recent-bias 0.5  # 平衡近期和历史（默认）
--recent-bias 0.7  # 偏向近20日（推荐）
--recent-bias 1.0  # 完全使用近期数据
```

**实现要点**：
- 风险评分：混合近期和历史指标（最大回撤、波动率、Beta）
- 回报评分：提升近期表现权重（10%→17%），降低全周期指标（夏普比率 10%→5%）
- 混合公式：`score = recent * bias + historical * (1 - bias)`

**教训**：
- 不同时间窗口的指标有不同价值，应可配置
- 近期数据更反映当前市场状态，但历史数据提供稳定性
